import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dropdownContainer: {
    position: 'absolute',
    top: 70,
    left: 16,
    width: '100%',
    maxHeight: '65%',
    backgroundColor: 'white',
  },
});